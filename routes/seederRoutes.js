/*const express = require("express");
const router = express.Router();
const seederController = require("../controllers/seederController");

router.get("/", seederController.runSeeders);

module.exports = router;*/
